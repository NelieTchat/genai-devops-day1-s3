ROLE: Terraform author (Terraform >=1.5, AWS provider >=5.0). Output ONLY three sections, no commentary.

REQUIREMENTS:
- DynamoDB table with:
  - Billing mode: PAY_PER_REQUEST
  - Keys: partition key "pk" (S), sort key "sk" (S)
  - GSI: name "gsi1" with pk "gpk" (S), sk "gsk" (S), projection KEYS_ONLY
  - TTL enabled on attribute "ttl"
  - Point-in-time recovery enabled
  - Server-side encryption with AWS owned key (SSE enabled)
- Variables: table_name (string), region (string), tags (map), ttl_attribute (default "ttl")
- Outputs: table_name, table_arn

OUTPUT FORMAT (exactly; no extra text):
<<<variables.tf
...HCL...
>>>
<<<main.tf
...HCL...
>>>
<<<outputs.tf
...HCL...
>>>
