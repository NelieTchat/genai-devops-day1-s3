ROLE: Expert Terraform (AWS >= 5.0, Terraform >= 1.5). Explicit inputs only.
TASK: Generate variables.tf, main.tf, outputs.tf for a private S3 bucket with:
- Versioning enabled
- SSE-KMS using input kms_key_id
- Lifecycle: noncurrent → STANDARD_IA at 30d; expire noncurrent at 180d
- Public access block
- Deny non-TLS and public canned ACLs
- Expose force_destroy
